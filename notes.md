#### Criando estrutura de projeto pré-configurada

    yarn create react-app <nome do projeto> --template typescript

#### Setting js with css

    yarn add styled-components

import styled from 'styled-components'

    yarn add @types/styled-components -D
